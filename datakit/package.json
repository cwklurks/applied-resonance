{
  "name": "earsight-datakit",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0 --port 5174",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview --host 0.0.0.0 --port 4174",
    "qr": "node scripts/lan-url.mjs",
    "test": "vitest run",
    "test:viewport": "playwright test",
    "test:real-save": "node scripts/real-save-check.mjs"
  },
  "devDependencies": {
    "@playwright/test": "^1.45.0",
    "qrcode-terminal": "^0.12.0",
    "typescript": "^5.7.0",
    "vite": "^8.0.16",
    "vitest": "^4.1.8"
  }
}
