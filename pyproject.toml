[project]
name = "earsight"
version = "0.1.0"
description = "Phase 0 MIMII acoustic-anomaly-detection baseline."
requires-python = ">=3.12,<3.13"
dependencies = [
    "torch>=2.4",
    "torchaudio>=2.4",
    "librosa>=0.10",
    "soundfile>=0.12",
    "scikit-learn>=1.4",
    "numpy>=1.26",
    "requests>=2.31",
    "tqdm>=4.66",
    "torchlibrosa>=0.1.0",
]

[dependency-groups]
dev = [
    "pytest>=8.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["engine"]

[tool.pytest.ini_options]
testpaths = ["tests"]
